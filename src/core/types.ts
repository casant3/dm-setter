/** Kept as the historical import path for the core's types, which now live in the shared domain module. */
export type { AgentResult, GateResult, Qualification, Review, Strategy } from "@/lib/types";
